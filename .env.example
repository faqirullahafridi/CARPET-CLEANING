# Required — PostgreSQL connection string (e.g. Neon, Supabase, or Vercel Postgres)
DATABASE_URL=

# Email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=Pureclean153@gmail.com
SMTP_PASS=
ADMIN_EMAIL=Pureclean153@gmail.com

# Evolution API — WhatsApp (run via scripts/setup-evolution.sh or docker-compose.evolution.yml)
# On Vercel, point EVOLUTION_BASE_URL to your public Evolution server (VPS), not localhost.
EVOLUTION_BASE_URL=http://localhost:8080
EVOLUTION_API_KEY=
EVOLUTION_INSTANCE=carpet-cleaning
# Admin alerts — UK business line (digits with country code, no +)
EVOLUTION_ADMIN_NUMBER=447533552015
# Optional — protect POST /api/evolution/test in production
EVOLUTION_TEST_KEY=

# Set automatically by vercel.json for builds
PORT=4173
BASE_PATH=/
